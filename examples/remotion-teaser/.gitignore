node_modules/
out/
.remotion/
*.log

# Regenerated by `npm run build:audio` — reproducible, not committed
public/audio/
# Regenerated by `npm run bench` — reproducible in any environment
public/benchmarks.json
# Lyria response cache (contains base64 music bytes) — regenerate from script
.cache/
